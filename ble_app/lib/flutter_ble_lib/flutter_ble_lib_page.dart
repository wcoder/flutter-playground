import 'package:flutter/material.dart';

class FlutterBleLibPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("flutter_ble_lib"),
      ),
      body: Text("flutter_ble_lib"),
    );
  }
}
